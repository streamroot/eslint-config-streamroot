machine:
    node:
        version: 6.0.0
    ruby:
        version: 2.2.0

dependencies:
    pre:
    override:
        - npm install

test:
    override:

deployment:
    master:
        branch: master
        commands:
            - git config --global user.email "test@streamroot.io"
            - git config --global user.name "Jean Hum"
            - ./node_modules/.bin/release-it major -n -p